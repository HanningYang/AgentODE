[
  {
    "type": "function",
    "function": {
      "name": "mean",
      "description": "Compute the arithmetic mean of a time-ordered series for a specified variable, computed per trajectory then averaged across trajectories.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "std",
      "description": "Compute the unbiased sample standard deviation (ddof=1) of a time-ordered series for a specified variable, computed per trajectory then averaged across trajectories.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "iqr",
      "description": "Compute the interquartile range (p75 - p25) of a time-ordered series for a specified variable, computed per trajectory then averaged across trajectories.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "skewness",
      "description": "Compute the Fisher-Pearson skewness of a time-ordered series for a specified variable, computed per trajectory then averaged across trajectories.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "outlier_frac",
      "description": "Compute the fraction of values beyond 2 standard deviations from the mean in a time-ordered series for a specified variable, computed per trajectory then averaged across trajectories.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "value_range",
      "description": "Compute the range (max - min) of a time-ordered series for a specified variable, computed per trajectory then averaged across trajectories.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_lag1",
      "description": "Compute lag-1 autocorrelation coefficient of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_lag2",
      "description": "Compute lag-2 autocorrelation coefficient of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_lag3",
      "description": "Compute lag-3 autocorrelation coefficient of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_first_zero",
      "description": "Compute the first lag where the autocorrelation crosses zero for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "dominant_freq",
      "description": "Compute the dominant frequency of a series via the power spectrum for a specified variable, in cycles per sample (normalized frequency).",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "spectral_entropy",
      "description": "Compute Shannon entropy of the normalized power spectrum for a specified variable, capturing frequency-domain complexity.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "statav",
      "description": "Compute variation of window means normalized by global standard deviation as a stationarity measure for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "std_first_diff",
      "description": "Compute the standard deviation of first differences of a time-ordered series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "perm_entropy_m3",
      "description": "Compute permutation entropy with embedding dimension m=3 for a specified variable, measuring ordinal complexity.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "lz_complexity",
      "description": "Compute normalized Lempel-Ziv complexity of a median-thresholded binary series for a specified variable, measuring symbolic sequence complexity.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "dfa_alpha",
      "description": "Estimate the detrended fluctuation analysis (DFA) scaling exponent for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "spectral_slope",
      "description": "Estimate the slope of log power versus log frequency in the power spectrum for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ar_coef_1",
      "description": "First autoregressive coefficient from an AR(3) model for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "exp_smooth_alpha",
      "description": "Fit simple exponential smoothing and return the estimated smoothing level alpha for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "turning_point_rate",
      "description": "Compute the rate of turning points (local maxima and minima) per sample for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "spearman_trend",
      "description": "Compute Spearman correlation between time index and series values as a trend measure for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mean_crossing_rate",
      "description": "Compute the fraction of time a series crosses its mean level for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mean_abs_diff",
      "description": "Compute the mean absolute first difference of a series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ccf_lag1",
      "description": "Compute Spearman cross-correlation at lag 1 between two variables: corr(x[t], y[t+1]). Measures how strongly x at time t predicts y one step ahead.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable_x": {
            "type": "string",
            "description": "The leading variable (x[t]). Must be one of: {variable_names}."
          },
          "variable_y": {
            "type": "string",
            "description": "The lagged variable (y[t+1]), must differ from variable_x. Must be one of: {variable_names}."
          }
        },
        "required": ["variable_x", "variable_y"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "level_corr",
      "description": "Compute Spearman correlation between the levels of two aligned variable time series.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable_x": {
            "type": "string",
            "description": "First variable. Must be one of: {variable_names}."
          },
          "variable_y": {
            "type": "string",
            "description": "Second variable, must differ from variable_x. Must be one of: {variable_names}."
          }
        },
        "required": ["variable_x", "variable_y"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "diff_corr",
      "description": "Compute Spearman correlation between first differences of two aligned variable time series.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable_x": {
            "type": "string",
            "description": "First variable. Must be one of: {variable_names}."
          },
          "variable_y": {
            "type": "string",
            "description": "Second variable, must differ from variable_x. Must be one of: {variable_names}."
          }
        },
        "required": ["variable_x", "variable_y"]
      }
    }
  }
]
