[
  {
    "type": "function",
    "function": {
      "name": "mean",
      "description": "Compute the arithmetic mean of a time-ordered series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "variance",
      "description": "Compute the unbiased sample variance (ddof=1) of a time-ordered series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "skewness",
      "description": "Compute Fisher-Pearson skewness of a time-ordered series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_lag1",
      "description": "Compute lag-1 autocorrelation coefficient of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_lag3",
      "description": "Compute lag-3 autocorrelation coefficient of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_1e_timescale",
      "description": "Compute the first lag where the autocorrelation falls below 1/e for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "dominant_frequency",
      "description": "Compute the dominant frequency of a series via the power spectrum for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "statav",
      "description": "Compute variation of window means normalized by global standard deviation as a stationarity measure for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "std_first_difference",
      "description": "Compute the standard deviation of first differences of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "permutation_entropy_m3",
      "description": "Compute permutation entropy with embedding dimension m=3 for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "time_reversal_asymmetry",
      "description": "Compute a time-reversal asymmetry statistic for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ar_coef_1",
      "description": "Compute the first autoregressive coefficient from a fitted AR model for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ar_coef_2",
      "description": "Compute the second autoregressive coefficient from a fitted AR model for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "turning_point_rate",
      "description": "Compute the rate of turning points per sample in a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of the available variables in the dataset."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "spearman_trend",
      "description": "Compute Spearman correlation between time index and series values as a trend measure for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of the available variables in the dataset."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "diff_corr",
      "description": "Compute Spearman correlation between first differences of two aligned variable time series.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable_x": {
            "type": "string",
            "description": "First variable. Must be one of: {variable_names}."
          },
          "variable_y": {
            "type": "string",
            "description": "Second variable, must differ from variable_x. Must be one of: {variable_names}."
          }
        },
        "required": ["variable_x", "variable_y"]
      }
    }
  }
]
