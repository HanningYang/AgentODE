[
  {
    "type": "function",
    "function": {
      "name": "within_patient_mean",
      "description": "Compute the arithmetic mean of a time-ordered series for a specified variable, computed per patient then averaged across patients.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "within_patient_variance",
      "description": "Compute the unbiased sample variance (ddof=1) of a time-ordered series for a specified variable, computed per patient then averaged across patients.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "within_patient_skewness",
      "description": "Compute Fisher-Pearson skewness of a time-ordered series for a specified variable, computed per patient then averaged across patients.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_lag1",
      "description": "Compute lag-1 autocorrelation coefficient of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_lag3",
      "description": "Compute lag-3 autocorrelation coefficient of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "acf_1e_timescale",
      "description": "Compute the first lag where the autocorrelation falls below 1/e for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "dominant_freq_normalized",
      "description": "Compute the dominant frequency of a series via the power spectrum for a specified variable, in cycles per sample (normalized frequency).",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "window_mean_variation_normalized",
      "description": "Compute variation of window means normalized by global standard deviation as a stationarity measure for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "std_first_diff_within",
      "description": "Compute the standard deviation of first differences of a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "permutation_entropy_m3",
      "description": "Compute permutation entropy with embedding dimension m=3 for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "time_reversal_asymmetry",
      "description": "Compute a time-reversal asymmetry statistic for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ar3_coef_1",
      "description": "Compute the first autoregressive coefficient from a fitted AR(3) model for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ar3_coef_2",
      "description": "Compute the second autoregressive coefficient from a fitted AR(3) model for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of: {variable_names}."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "turning_point_rate",
      "description": "Compute the rate of turning points per sample in a time series for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of the available variables in the dataset."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "spearman_trend",
      "description": "Compute Spearman correlation between time index and series values as a trend measure for a specified variable.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable": {
            "type": "string",
            "description": "The variable to compute the statistic for. Must be one of the available variables in the dataset."
          }
        },
        "required": ["variable"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "first_diff_spearman_corr",
      "description": "Compute Spearman correlation between first differences of two aligned variable time series.",
      "parameters": {
        "type": "object",
        "properties": {
          "variable_x": {
            "type": "string",
            "description": "First variable. Must be one of: {variable_names}."
          },
          "variable_y": {
            "type": "string",
            "description": "Second variable, must differ from variable_x. Must be one of: {variable_names}."
          }
        },
        "required": ["variable_x", "variable_y"]
      }
    }
  }
]
