### COMMON_INSTRUCTIONS_BEFORE
You are optimizing moderately informative physiological priors for the parameters of a mechanistic ODE model describing Creatinine (mg/dL), BUN (mg/dL), and Potassium (mmol/L) trajectories for acute kidney injury (AKI) over the first 7 days after hospital admission (t in hours; 0–168).
Your goal is to adjust the prior mean and SD in linear space for each parameter to make simulated summary statistics better match observed summaries, while keeping parameters physiologically plausible and allowing substantial inter-patient variability.
"Moderately informative" means: the prior should rule out physiologically implausible values but remain broad enough to accommodate substantial inter-patient variability.

Biomarkers:
Creatinine (mg/dL) – Indicator of renal filtration function
BUN – Blood Urea Nitrogen (mg/dL) – Accumulates when renal clearance fails
Potassium (mmol/L) – Reflects renal electrolyte regulation and impaired excretion


ODE System 


Current parameter distributions (lognormal, values in linear space)



### MODE_IMPLAUSIBLE
Some parameter settings produce implausible or invalid trajectories (e.g., NaNs, negative biomarker values, solver blow-ups, or values outside acceptable physiological ranges). 
It signals that the priors are placing too much mass in unstable/implausible regions. Your job is to update priors to increase the fraction of valid trajectories while remaining moderately informative.
Implausibility report (provided below):


### MODE_LOG_SL
We compare the simulated data with observed data using followed summary statistics per biomarker:
1. Quantile polynomial coefficients: Linear fit (deg0, deg1) to the empirical quantile function of raw biomarker values pooled across patients and time (marginal shape).
2. Lag-1 autocorrelation (Spearman) between consecutive time points (temporal persistence).
3. Population trend (Spearman) between time and population mean trajectory (direction).
4. Difference correlations (3): Spearman correlation of first-differences between biomarker pairs (dynamic coupling).
Current top 5 problems (provided below):


### COMMON_INSTRUCTIONS_AFTER
What you must do
For each parameter index:
1. Infer physiological meaning from the ODE role and AKI pathophysiology.
2. Ensure calibration for hourly timescale (explicitly check units).
3. Propose updated mean and SD in linear space.
4. Propose updated parameter distributions (mean and std for each parameter) to generate plausible trajectories and improve the log-likelihood. 
5. Output only: (1) a brief explanation of each change. (2) a JSON object where the keys are parameter indices. Each parameter entry must include: "mean", "sd", and "rationale" (1 sentence).

JSON Output Format (strictly)
{
    "0": { "mean": ..., "sd": ..., "rationale": "..." },
    "1": { "mean": ..., "sd": ..., "rationale": "..." },
    "2": { "mean": ..., "sd": ..., "rationale": "..." },
    ...
}



