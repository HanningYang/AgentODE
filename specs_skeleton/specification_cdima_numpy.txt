"""
Find the symbolic skeleton of an ODE system representing
a two-dimensional activator-inhibitor chemical reaction system.

The system involves two coupled state variables:

    - activator (concentration): Activator species
    - inhibitor (concentration): Inhibitor species

The time unit is dimensionless. Observations span t in [0, 40.0]
with 101 timepoints (dt = 0.4).

HARD CONSTRAINT:
- The parameter array has at most MAX_NPARAMS = 6 slots.
- Do NOT skip indices.
- Use only as many parameters as necessary; avoid introducing
  redundant parameters.
- You must not use any params[k] with k >= MAX_NPARAMS.
"""

import numpy as np

MAX_NPARAMS = 6
params = [1.0] * MAX_NPARAMS


@system.evolve
def system(biomarkers: np.ndarray, params: np.ndarray) -> np.ndarray:
    """Symbolic skeleton for a 2-variable ODE system.

    Args:
        biomarkers: A numpy array containing [activator, inhibitor].
        params: Array of numeric constants or parameters to be optimized.
                You must not use any params[k] with k >= MAX_NPARAMS.
    Returns:
        A numpy array representing the temporal derivatives (d/dt)
        of the state variables [d_activator/dt, d_inhibitor/dt].
    """
    activator, inhibitor = biomarkers

    # ---- activator dynamics ----
    d_activator = params[0] * activator

    # ---- inhibitor dynamics ----
    d_inhibitor = params[1] * inhibitor

    d_biomarkers = np.array([d_activator, d_inhibitor], dtype=float)
    return d_biomarkers