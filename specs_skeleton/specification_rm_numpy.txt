"""
Find the symbolic skeleton of an ODE system representing
a two-variable population dynamics model.

The system involves two coupled state variables:
    - x0 (density, ind/m^2): Population A
    - x1 (density, ind/m^2): Population B

The time unit is days. Observations span t in [0, 50.0].

HARD CONSTRAINT:
- The parameter array has at most MAX_NPARAMS = 8 slots.
- Do NOT skip indices.
- Use only as many parameters as necessary; avoid introducing
  redundant parameters.
- You must not use any params[k] with k >= MAX_NPARAMS.
"""
import numpy as np

MAX_NPARAMS = 8
params = [1.0] * MAX_NPARAMS

@system.evolve
def system(biomarkers: np.ndarray, params: np.ndarray) -> np.ndarray:
    """Symbolic skeleton for a 2-variable ODE system.

    Args:
        biomarkers: A numpy array containing [x0, x1].
        params: Array of numeric constants or parameters to be optimized.
                You must not use any params[k] with k >= MAX_NPARAMS.

    Returns:
        A numpy array representing the temporal derivatives (d/dt)
        of the state variables [dx0/dt, dx1/dt].
    """
    x0, x1 = biomarkers

    # ---- x0 dynamics ----
    dx0 = params[0] * x0 * (1 - x0/params[1]) - params[2] * x0 / (1 + params[2] * params[3] * x0) * x1

    # ---- x1 dynamics ----
    dx1 = params[4] * params[2] * x0 / (1 + params[2] * params[3] * x0) * x1 - params[5] * x1

    d_biomarkers = np.array([dx0, dx1], dtype=float)
    return d_biomarkers