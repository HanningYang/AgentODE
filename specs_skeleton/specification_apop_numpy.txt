"""
Find the symbolic skeleton of an ODE system representing
a three-variable caspase activation cascade.

The system involves three coupled state variables:

- initiator: upstream caspase activity/concentration
- regulator: intermediate regulatory component
- effector: downstream effector caspase activity/concentration


The naming reflects their role in the cascade, but interactions
do not need to be strictly feedforward.

The time unit is dimensionless. Observations span t in [0, 10.0]

HARD CONSTRAINT:
- The parameter array has at most MAX_NPARAMS = 10 slots.
- Do NOT skip indices.
- Use only as many parameters as necessary; avoid introducing
  redundant parameters.
- You must not use any params[k] with k >= MAX_NPARAMS.
"""

import numpy as np

MAX_NPARAMS = 10
params = [1.0] * MAX_NPARAMS


@system.evolve
def system(biomarkers: np.ndarray, params: np.ndarray) -> np.ndarray:
    """Symbolic skeleton for a 3-variable ODE system.

    Args:
        biomarkers: A numpy array containing
                    [initiator, regulator, effector].
        params: Array of numeric constants or parameters to be optimized.
                You must not use any params[k] with k >= MAX_NPARAMS.
    Returns:
        A numpy array representing the temporal derivatives (d/dt)
        of the state variables
        [d_initiator/dt, d_regulator/dt, d_effector/dt].
    """
    initiator, regulator, effector = biomarkers

    # ---- initiator dynamics ----
    d_initiator = 0.0

    # ---- regulator dynamics ----
    d_regulator = 0.0

    # ---- effector dynamics ----
    d_effector = 0.0

    d_biomarkers = np.array([d_initiator, d_regulator, d_effector],
                            dtype=float)
    return d_biomarkers